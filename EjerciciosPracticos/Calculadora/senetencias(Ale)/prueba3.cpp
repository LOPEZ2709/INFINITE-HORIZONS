#include <iostream>
using namespace std;

int main() {
    int contador = 0;
    do {
        cout << "Contador: " << contador << endl;
        contador++;
    } while (contador < 5);
    return 0;
}